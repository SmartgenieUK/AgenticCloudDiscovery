# MCP Server Configuration

# Server
MCP_HOST=0.0.0.0
MCP_PORT=9000
LOG_LEVEL=INFO

# Cosmos DB (optional - will use in-memory if not set)
COSMOS_ENDPOINT=
COSMOS_KEY=
COSMOS_DATABASE=AgenticCloudDiscovery

# APIM Configuration
APIM_BASE_URL=
APIM_STUB_MODE=true
APIM_TIMEOUT_SECONDS=30

# Policy Defaults (used if no policy in Cosmos)
DEFAULT_MAX_PAYLOAD_BYTES=10485760
DEFAULT_MAX_RETRIES=3
DEFAULT_APPROVAL_REQUIRED=true
